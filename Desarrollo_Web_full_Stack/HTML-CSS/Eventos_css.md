# Eventos CSS.

Es cuando el usuario interactua con algun elemento de la web, ya sea poniendo el cursor encima, también son llamados eventos de css o pseudoclases ya que son clases que se usan en el momento donde se pasa el cursor u otros eventos similares:

Para usarlos se debe tener en cuenta la sintaxis, donde despues de llamar la clase o el identificador, se usa ":" y se especifica el evento a usar:

Pon un ejemplo:
